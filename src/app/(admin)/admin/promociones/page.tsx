import { AdminPromotions } from "@/components/admin/AdminPromotions";

export const metadata = { title: "Promociones — Admin Compuelite" };

export default function PromocionesPage() {
  return <AdminPromotions />;
}
